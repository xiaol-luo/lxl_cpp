
local function parse_cmd_arg(out_ret)
    out_ret = out_ret or {}

    parse_fns = {}
    parse_fns["-package_path"] = function(args, arg_idx, ret)

    end
    parse_fns["-package_cpath"] = function(args, arg_idx, ret)

    end

    for arg_idx=1, #arg do
        curr_arg = arg[arg_idx]
        
    end
end

