
ProtoStoreBase = ProtoStoreBase or class("ProtoStoreBase")

function ProtoStoreBase:add_search_dirs(search_dirs)
    assert(false, "should not reach here")
end

function ProtoStoreBase:load_files(files)
    assert(false, "should not reach here")
end

function ProtoStoreBase:encode(proto_name, param)
    assert(false, "should not reach here")
end

function ProtoStoreBase:decode(proto_name, param)
    assert(false, "should not reach here")
end